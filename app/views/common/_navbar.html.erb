<nav class="navbar navbar-default navbar-fixed-top" id="myScrollspy">
    <div class="container-fluid">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header">
        <a class="navbar-brand" href="/"><img src='<%= image_url 'logo.png' %>' >孤獨的閱讀者</a>
      </div>


      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <li>
                   <%= link_to("关于我们", about_path) %> <!-- 此处也可以使用一个外部连接-->
                <% if !current_user %>
                    <li><%= link_to("注册", new_user_registration_path) %> </li>
                    <li><%= link_to("登录", new_user_session_path) %></li>
                <% else %>
                   <li class="dropdown">
                     <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                         Hi!, <%= current_user.email %>
                         <b class="caret"></b>
                     </a>
                     <ul class="dropdown-menu">
                         <% if current_user.admin? %>
                         <li> <%= link_to("管理员后台", admin_jobs_path) %> </li>
                         <% end %>
                         <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
                     </ul>
                   </li>
                 <% end %>
                </li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>




<!-- 这是从lonelyreader上参考的
<div class="ui modal small transition visible active" id="sign-up" style="margin-top: -249px; display: block !important;">
  <i class="close icon"></i>
    <div class="header ui-red-bg ui-text-center">注册账号</div>
      <div class="content">
        <div class="ui form" style="margin-top:30px">
          <div class="inline field">
            <div class="ui input" ng-class="{disabled:step==2}">
              <input type="text" placeholder="你的工作邮箱或手机号" ng-model="signParams.username" value="" class="ng-pristine ng-valid ng-touched">
            </div>
          </div>
          <div class="ui divider hidden"></div>
          <div class="inline field">
            <div class="ui input">
              <div class="ui input" ng-class="{disabled:step==2}">
                <input type="password" placeholder="密码" ng-model="signParams.password" class="ng-pristine ng-untouched ng-valid">
              </div>
            </div>
          </div>
          <div class="ui divider hidden"></div>
          <div class="inline field">
            <div class="ui input">
              <div class="ui input" ng-class="{disabled:step==2}">
                <input type="password" placeholder="确认密码" ng-model="signParams.rePassword" class="ng-pristine ng-untouched ng-valid">
              </div>
            </div>
          </div>
          <div class="ui divider hidden"></div>
          <div class="inline field">
            <div class="ui action input">
              <div class="ui right labeled input primary">
                <input type="text" placeholder="请输入验证码" ng-model="signParams.captcha" class="ng-pristine ng-untouched ng-valid">
                <div class="ui button ui-red-bg" ng-click="getCaptcha()" ng-class="{'disabled':isSend,'loading':isSaveInfoPosted2}">
                  <span ng-hide="isSend||isSaveInfoPosted2" style="color:#fff">获取验证码</span>
                  <span ng-show="isSend" style="color:#fff" class="ng-binding ng-hide">120</span>
                </div>
              </div>
            </div>
          </div>
          <div class="ui divider hidden">
          </div><div class="inline field">
            <button class="ui animated button primary ui-red-bg fluid" ng-click="signUp()" ng-class="{disabled:isSaveInfoPosted,loading:isSaveInfoPosted}">
              <div class="visible content">立即加入</div><div class="hidden content">
                <i class="right arrow icon"></i>
              </div>
            </button>
          </div>
          <div class="inline field" style="padding:0 15%">
            <span>已有账号？
              <a class="showLogin">立即登录</a>
            </span>
          </div>
          <div class="ui divider hidden">
          </div>
        </div>
      </div>
    </div>
-->
